query log($id: ID) {
  log(id: $id) {
    _id
    startTime
    endTime
    duration
    distance
    valid
    points
    gpsPoints {
      _id
      time
      latitude
      longitude
      altitude
    }
  }
}
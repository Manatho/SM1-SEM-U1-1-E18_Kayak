query log($id: ID) {
  log(id: $id) {
    _id
    startTime
    endTime
    duration
    distance
    points
    gpsPoints {
      _id
      time
      latitude
      longitude
      altitude
      valid
      speed
    }
  }
}
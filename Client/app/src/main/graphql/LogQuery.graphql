query log($id: Int) {
  log(id: $id) {
    id
    startTime
    endTime
    duration
    distance
    valid
    points
    GPSPoints {
      id
      latitude
      longitude
      altitude
    }
  }
}